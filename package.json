{
  "name": "vereins-app",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "lint": "eslint src/*.jsx src/*.js",
    "smoke": "node scripts/smoke.mjs",
    "verify": "npm run lint && npm test && npm run build && node scripts/smoke.mjs && node scripts/sweep.mjs"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "eslint": "^9.39.4",
    "eslint-plugin-react": "^7.37.5",
    "playwright-core": "^1.61.1",
    "web-push": "^3.6.7"
  }
}